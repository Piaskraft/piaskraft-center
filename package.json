{
  "name": "piaskraft-center",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix apps/web\" \"npm run start:dev --prefix apps/api\"",
    "dev:web": "npm run dev --prefix apps/web",
    "dev:api": "npm run start:dev --prefix apps/api"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}